server:
  port: 3210
  address: 0.0.0.0

app:
  allowedOrigins: '*'
  ignoreSession: true
  authUri: https://graph.microsoft.com/v1.0/me?$select=employeeId

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      base-path: /

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/Coatepec06052026
    username: postgres
    password: 123
    hikari:
      minimumIdle: 1
      maximumPoolSize: 4
      idleTimeout: 15000
      poolName: SpringBootJPAHikariCP
      maxLifetime: 1800000
      connectionTimeout: 120000
  sql:
    init:
      encoding: UTF-8
  jpa:
    open-in-view: false
    properties:
      hibernate:
        ddl-auto: none